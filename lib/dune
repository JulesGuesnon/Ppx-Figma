(library
    (name lib)
    (public_name bs-figma-ppx)
    (kind ppx_rewriter)
    (libraries ppxlib ssl cohttp cohttp-lwt-unix lwt lwt.unix yojson str)
    (preprocess (pps ppxlib.metaquot))
    ;;(flags (:standard -dsource))
)

(include_subdirs unqualified)